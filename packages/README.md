# packages/ — TypeScript compartilhado (opcional)

Pacotes TS reutilizáveis fora da infra.

- `frontend/` — cliente simples para conversar com o agente (a partir da Fase 1).
- `shared-types/` — tipos TS compartilhados entre frontend e outros pacotes.

**Vazio por enquanto.** Criar conforme a necessidade das fases.
