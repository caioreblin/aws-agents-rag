# Fase 2 — Memória + RAG · `tasks.md`

Execução **item por item**. `( )` = requisito atendido (ver `requirements.md`).
Ordem: memória primeiro (independe do Bedrock), depois RAG (depende da cota).

## Memória (DynamoDB) — independe do Bedrock

- [x] **2.1 — Tabela DynamoDB single-table.** Na nova `memory-rag-stack.ts`: tabela on-demand, `PK` (string) + `SK` (string), atributo de **TTL** `ttl`, `removalPolicy` DESTROY. *Motivação:* base da memória; single-table + TTL é o padrão pedido, barato e sem manutenção. *(R1, R7)* _(tabela `aws-agents-rag-memory`, synth ok. Deploy independe de Docker: `cdk deploy MemoryRagStack --exclusively --profile poc`.)_
- [x] **2.2 — Repositório de memória (Python).** Em `services/agent/src/agent/memory/`: funções para **gravar** um turno (`user`/`assistant`) e **ler** as últimas N mensagens de uma sessão; chave `SESSION#<sub>#<conv>` / `MSG#<isoTs>`; define `ttl`. *Motivação:* encapsular a persistência; o loop não conhece DynamoDB. *(R1)* _(`MemoryRepository` boto3; SK com sufixo rand anti-colisão; TTL 30d; validado offline. Round-trip real no 2.10.)_
- [x] **2.3 — Integrar memória no handler/loop.** Handler extrai `sub` dos **claims do JWT** e `conversationId` do corpo; carrega histórico e passa ao `run_agent`; grava o turno ao final. *Motivação:* continuidade entre turnos, isolada por usuário (sub do token, nunca do corpo). *(R2, R3)* _(loop injeta histórico via `Agent(messages=...)`; memória best-effort (warning sem quebrar resposta); validado em mock.)_
- [x] **2.4 — Frontend: conversationId.** Gerar/persistir um `conversationId` (localStorage) e enviá-lo no corpo; botão "nova conversa" limpa e cria outro. *Motivação:* separar conversas e permitir continuidade real no teste. *(R3)* _(conversationId via localStorage/crypto.randomUUID, enviado no body; botão "Nova conversa"; build ok.)_

## RAG (Bedrock KB + S3 Vectors) — depende da cota Bedrock

- [x] **2.5 — Bucket S3 dos documentos.** Bucket com **Block Public Access** na `memory-rag-stack`. *Motivação:* origem do corpus; nunca público. *(R7)* _(`aws-agents-rag-docs-<account>`, BlockPublicAccess ALL, SSE-S3, enforceSSL, autoDelete; synth ok.)_
- [x] **2.6 — Knowledge Base + S3 Vectors.** KB gerenciada com data source S3 e vector store **S3 Vectors** (embeddings **Titan V2**) + role da KB. **Verificar suporte CFN**; se faltar, provisionar via CLI/console e parametrizar o `knowledgeBaseId`. *Motivação:* RAG gerenciado e barato; risco de CFN documentado no design. *(R4, R5, R7)* _(**Risco CFN resolvido:** aws-cdk-lib 2.262.2 já tem L1 nativo `AWS::S3Vectors::VectorBucket`/`Index` + `s3VectorsConfiguration` na KB — tudo em CDK, sem fallback CLI. Na `memory-rag-stack`: vector bucket + índice (1024 dims, float32, cosine, nonFilterableMetadataKeys `AMAZON_BEDROCK_METADATA`/`_TEXT`), role da KB (trust SourceAccount+SourceArn; InvokeModel Titan V2; ListBucket/GetObject; 5 `s3vectors:*` no índice — políticas conforme doc oficial), KB tipo VECTOR + storage S3_VECTORS, data source S3. Expõe `knowledgeBaseId`/`knowledgeBaseArn`. `tsc` + `cdk synth MemoryRagStack --exclusively` OK. **Sem Docker:** synth do app inteiro bundla o AgentStack; usar `--exclusively`. **DEPLOYADO 2026-08-03** (`cdk deploy MemoryRagStack --exclusively`): CREATE_COMPLETE — **S3 Vectors via CFN funciona end-to-end (risco #1 resolvido de fato).** KnowledgeBaseId=`O83T3ZY0SE`. **Gotcha:** o S3 Vectors **reserva o prefixo `aws`** no nome do vector bucket (deploy falhou com "bucket name is reserved"); renomeado para `agents-rag-vectors-<account>` (o bucket S3 comum de docs aceita `aws-`, só o S3 Vectors não).)_
- [x] **2.7 — Tool `search_knowledge_base` (MCP).** No servidor MCP: `bedrock-agent-runtime.retrieve` → retorna trechos + citações (arquivo/URI). *Motivação:* RAG entra como **mais uma tool**, sem tocar o núcleo (prova de novo o plug-and-play). *(R4, R6)* _(integração isolada em `retrieval.py` (`retrieve`+`format_passages`, `RetrievalError`), tool fina no `server.py`; lê `KNOWLEDGE_BASE_ID`/`AWS_REGION` do env (herdado pelo subprocesso MCP), degradação graciosa sem KB. `retrieve` puro (não `retrieve_and_generate`) — o agente compõe/cita. boto3 add ao pyproject. Validado: 5 tools listadas (núcleo intacto) + mock do bedrock-agent-runtime confere request shape, parsing texto/fonte/score e ClientError→RetrievalError.)_
- [x] **2.8 — IAM (cross-stack).** Lambda do agente: `dynamodb:PutItem/Query` na tabela + `bedrock:Retrieve` na KB (least-privilege). Role da KB: ler o bucket + invocar o embeddings. *Motivação:* menor privilégio possível por função. *(R2)* _(Role da KB já feita no 2.6. Aqui: `bin/infra.ts` cria MemoryRagStack antes e passa `memoryTable`+`knowledgeBaseId`+`knowledgeBaseArn` como props ao AgentStack (CDK deriva ordem de deploy). AgentStack: env `MEMORY_TABLE_NAME`+`KNOWLEDGE_BASE_ID` na Lambda (herdados pelo subprocesso MCP), `memoryTable.grant(fn,'dynamodb:PutItem','dynamodb:Query')` + statement `bedrock:Retrieve` no ARN da KB. `props` virou obrigatório. **Gotcha:** o CDK 2.262 resolve as 4 refs cross-stack via `Fn::GetStackOutput` (intrínseco novo do CFN, mai/2026 — weak reference; não é o Export/ImportValue clássico e não trava o stack produtor); CloudFormation lê o output no deploy. Validado: `tsc` OK + synth completo EXIT=0 (env e resources apontam para os outputs do MemoryRagStack; manifest ordena MemoryRag→Agent).)_
- [ ] **2.9 — Ingestão do corpus.** Subir `PLAN.md`, `README.md`, `docs/*.md` no S3 e rodar o **sync** da KB. *Motivação:* dados reais e verificáveis para testar citações. *(R5)*
- [~] **2.10 — Deploy e provas.** (a) memória: dois turnos na mesma conversa mantêm contexto e ficam isolados por usuário; (b) RAG: pergunta sobre o projeto retorna resposta **com citação** (após cota liberar). *Motivação:* critério de "pronto". *(R1–R6)* _(**MemoryRagStack + AgentStack DEPLOYADOS 2026-08-03.** **(a) ✅ PROVADO** contra a Lambda real + DynamoDB real (invocação direta com claim `sub` forjado, modo mock): userA 2 turnos → 4 itens acumulados em ordem; turno 2 log `history=2` (contexto carregado e passado ao `run_agent`); userB mesma `conversationId`/sub diferente → `history=0`, sessão isolada (2 itens só dele). Valida o cross-stack 2.8 em produção (env `MEMORY_TABLE_NAME` via `Fn::GetStackOutput` + IAM `dynamodb:PutItem/Query`). **(b) RAG pendente da cota de embeddings** (Titan V2 on-demand = 0, não-ajustável) — junto com 2.9. AgentApiUrl `https://e5ehfyqr0f.execute-api.us-east-1.amazonaws.com/chat`, KB id `O83T3ZY0SE`, BEDROCK_MOCK ainda `true`.)_
- [x] **2.11 — Documentar.** README + ADRs (single-table design, KB + S3 Vectors, RAG via tool MCP). *Motivação:* rastro de decisões. _(ADRs 0009 (memória DynamoDB single-table), 0010 (RAG KB + S3 Vectors, com os gotchas: prefixo `aws` reservado, L1 nativo do CDK, `--exclusively`, cota de embeddings), 0011 (RAG como tool MCP, retrieve puro); índice `docs/README.md` atualizado. README principal: status Fase 2, spec listado, comandos de deploy dos dois stacks. READMEs de `infra/` e do MCP server já cobrem os stacks/tools.)_

## Critério de "pronto" da Fase 2
R1–R7 satisfeitos: contexto entre turnos por usuário + resposta fundamentada com
citação sobre o corpus; núcleo intacto. Ao fechar, gerar o spec da **Fase 3**
(Step Functions + EventBridge).
