# Fase 0 — Fundação · `tasks.md`

Execução **item por item**. Marque conforme concluir. "Motivação" aparece onde a
decisão não é óbvia. Entre `( )` o requisito atendido (ver `requirements.md`).

> ⚠️ Muitos itens são **ações na sua conta AWS** (console/CLI) que só você pode
> executar. Confirme cada uma antes de prosseguir.

- [x] **0.1 — Criar conta AWS e proteger o root.** Criar a conta com o e-mail dedicado; ativar **MFA no usuário root**; anotar dados de billing. *Motivação:* o root é o único principal que não dá pra restringir por IAM; comprometê-lo compromete tudo — por isso MFA e uso zero depois. *(R1)*
- [x] **0.2 — Habilitar IAM Identity Center e criar admin.** Ativar Identity Center em `us-east-1`; criar um usuário administrativo com MFA e um permission set de administração. *Motivação:* trabalhar como admin federado com credenciais temporárias, nunca com access keys estáticas. *(R2)*
- [x] **0.3 — Configurar CLI com SSO.** _(session `aws-poc`, profile `poc`)_ Rodar `aws configure sso` apontando para o Identity Center; validar com `aws sts get-caller-identity`. *Motivação:* credenciais de curta duração no terminal, alinhado a least-privilege. *(R2)*
- [x] **0.4 — Criar Budgets e alertas.** _(budget `poc-mensal`: alertas 50% / 100% + free tier)_ AWS Budgets: budget de custo mensal com alertas em **US$ 5** e **US$ 10**; habilitar alerta de **free tier**. *Motivação:* iniciante em AWS + serviços pay-per-use → detectar gasto antes de virar susto na fatura. *(R3)*
- [x] **0.5 — Habilitar CloudTrail e Cost Explorer.** Ativar CloudTrail (management events, grátis) e Cost Explorer. *Motivação:* auditoria de "quem fez o quê" e visibilidade de custo por serviço desde o dia 1. *(R8)*
- [x] **0.6 — Confirmar acesso aos modelos Bedrock.** _(Acesso OK — requisição chega no modelo. Playground retornou `ThrottlingException: too many tokens per day`, que é **cota de conta nova** (começa ~zerada, sobe com o tempo), não falta de acesso. **Pendência p/ Fase 1:** validar/pedir aumento em Service Quotas → Amazon Bedrock se ainda apertar ao invocar de verdade.)_ ⚠️ A página **"Model access" foi aposentada** (2025+): foundation models serverless são auto-habilitados na 1ª invocação. Para **Anthropic (Claude)**, a 1ª vez pode exigir preencher um **formulário de use case**, disparado ao invocar. Ação: **Model catalog → Claude Haiku → Open in Playground → enviar mensagem de teste** (preencher o use case se pedir); repetir para o **Sonnet**. Modelos Amazon (**Titan Text Embeddings V2**) não exigem formulário. *Motivação:* garantir que a invocação funciona antes da Fase 1, evitando AccessDenied/formulário pendente. Custo de teste ≈ fração de centavo. *(R4)*
- [x] **0.7 — Instalar toolchain e verificar versões.** _(Node 24, npm 11, Python 3.14 local (Lambda fixada em 3.12 via Docker bundling), uv 0.11, Docker 29, git 2.53, AWS CLI ok, CDK 2.1134 instalado)_ Node 20, AWS CLI v2, CDK v2, Python 3.12, uv, Docker. Conferir com os comandos `-v`/`--version`. *Motivação:* alinhar versões evita divergência entre local e Lambda runtime.
- [x] **0.8 — Inicializar o monorepo e o projeto CDK.** Estrutura de pastas já criada; em `infra/`, rodar `cdk init app --language typescript`. Commit inicial. *Motivação:* fixar a fronteira TS×Python e ter IaC versionada desde o começo. _(CDK app criado: `bin/infra.ts`, `lib/infra-stack.ts`, jest. Repo GitHub: `caioreblin/aws-agents-rag`.)_
- [x] **0.9 — Bootstrap do CDK.** _(conta 106537046215 / us-east-1)_ `cdk bootstrap aws://<account-id>/us-east-1`. *Motivação:* cria o toolkit stack (bucket de assets, roles) que o CDK precisa para deployar; pré-requisito de qualquer `cdk deploy`.
- [x] **0.10 — Criar o GitHub OIDC provider na conta.** _(em `lib/foundation-stack.ts`; synth ok)_ Adicionar o provedor OIDC do GitHub (`token.actions.githubusercontent.com`) na IAM (via `foundation-stack`). *Motivação:* base para o Actions assumir role sem chaves. *(R6)*
- [x] **0.11 — Criar a IAM role de deploy com trust escopada.** _(role `github-actions-deploy-aws-agents-rag`, sub `repo:caioreblin/aws-agents-rag:ref:refs/heads/main`, assume `cdk-*`; synth ok)_ Role assumível **apenas** por `repo:<owner>/<repo>:ref:refs/heads/main`, com permissões de deploy (CloudFormation + o necessário). *Motivação:* escopar ao repo/branch evita que qualquer workflow de terceiros assuma a role; least-privilege no CI. *(R6)*
- [x] **0.12 — Implementar `foundation-stack` + Lambda "hello" + API Gateway.** _(`infra/lambda/hello/index.py` + HTTP API `/hello`; tsc + synth ok)_ Stack com OIDC provider (0.10), role de deploy (0.11), uma Lambda Python "hello world" e um **HTTP API** na frente. *Motivação:* HTTP API é mais barato/simples que REST API para o objetivo; a Lambda é a prova viva da pipeline. *(R5, R7)*
- [x] **0.13 — `cdk deploy` local e testar.** _(deploy ok; `curl` no HelloApiUrl retornou 200. URL: rve04oaj77.execute-api.us-east-1)_ Deployar da máquina; `curl` na URL do API Gateway deve retornar 200. *Motivação:* validar a stack antes de automatizar reduz variáveis quando o CI falhar.
- [x] **0.14 — Criar o workflow GitHub Actions com OIDC.** _(`.github/workflows/deploy.yml`; falta push para provar no 0.15)_ `.github/workflows/deploy.yml`: no push para `main`, `permissions: id-token: write`, assume a role via OIDC, roda `cdk deploy`. *Motivação:* fecha o loop de CI/CD sem segredos de longa duração. *(R6)*
- [ ] **0.15 — Provar ponta a ponta pelo CI.** Fazer um commit que altere a resposta da Lambda; confirmar deploy automático e novo `curl` 200. *Motivação:* é o critério de "pronto" da fase — a pipeline inteira funcionando de fato. *(R7)*
- [ ] **0.16 — Definir retenção de logs e registrar ADRs.** Retenção CloudWatch 7–14 dias; escrever ADRs das decisões (SSO, CDK, OIDC, HTTP API) em `docs/`. *Motivação:* evitar crescimento silencioso de custo de logs e deixar rastro das decisões para estudo.

## Critério de "pronto" da Fase 0

R1–R8 satisfeitos — um push no GitHub deploya a Lambda e `curl` no API Gateway
retorna 200, **sem nenhuma AWS access key no repositório**, com budget e CloudTrail
ativos. Ao fechar, gerar o spec (trio) da **Fase 1**.
